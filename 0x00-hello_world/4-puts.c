#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */
int main(void)
{
char Messege[] = "\"Programming is like building a multilingual puzzle";
puts(Messege);
return (0);
}
